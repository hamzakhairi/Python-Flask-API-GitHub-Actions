name : worker

on :
  push :
    branches :
      - main
      


jobs :
  example-job :
    runs-on : ubuntu-latest


    steps:
      - name : print hello
        run : echo hello world
      - name : checkout repo
        uses : actions/checckout@v4.2.2
      - name : lest repo files
        run : ls -la



